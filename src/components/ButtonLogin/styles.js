import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonContainer: {
        backgroundColor: "#573F94",
        borderColor: "#ffffff",
        borderWidth: 1,
        borderRadius: 5,
        paddingVertical: 10,
    },
    buttonText: {
        color: "#ffffff",
        fontSize: 16,
        textAlign: "center",
    }
});
